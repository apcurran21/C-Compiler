void main() {
:entrance
h()
e()
l()
l()
o()
space()
w()
o()
r()
l()
d()
return
}

void h(){
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 209
    print(var)
    return
}

void e(){
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 203
    print(var)
    return
}

void l(){
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 217
    print(var)
    return
}

void o() {
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 223
    print(var)
    return
}

void w(){
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 239
    print(var)
    return
}

void space(){
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 65
    print(var)
    return
}

void r(){
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 229
    print(var)
    return
}

void d(){
    :entrance
    int64 var
    //pass in first argument (h in ASCII) according to calling convention
    var <- 201
    print(var)
    return
}
