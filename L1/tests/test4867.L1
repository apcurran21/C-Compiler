(@go
 (@go
  0 0
  rdi <- 5
  rsi <- 7
  call allocate 2
  mem rax 8 <- 11
  mem rax 16 <- 13
  rdi <- 7
  rsi <- rax
  // at the same array
  call allocate 2
  rdi <- rax
  call print 1
  return
)
)
