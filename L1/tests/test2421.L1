(@main
(@main
	0 1
	rdi <- 1
	mem rsp 0 <- rdi
	:mainloop
	mem rsp -8 <- :ret1
	rdi <- mem rsp 0
	rdi <- rdi
	cjump 21 < rdi :end
	call @fib 1
	:ret1
	rdi <- rax
	rdi <<= 1
	rdi += 1
	call print 1
	rdi <- mem rsp 0
	rdi += 1
	mem rsp 0 <- rdi
	goto :mainloop
	:end
	return
)

(@fib
	1 0
	rax <- 1
	rsi <- 0
	rdi <- rdi
	:fibloop
	rdx <- rax
	rax += rsi
	rsi <- rdx
	rdi--
	cjump 0 < rdi :fibloop
	return
)

)
