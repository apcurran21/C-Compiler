(@main
(@main
	0 4
	:call_label0
	rdi <- 31
	mem rsp 16 <- rdi
	rdi <- 3
	mem rsp 0 <- rdi
	rdi <- mem rsp 16
	rdi <- rdi
	rdi >>= 1
	rsi <- rdi
	rdi <- mem rsp 0
	rdi <- rdi
	rdi >>= 1
	rsi <- rsi
	rsi *= rdi
	rsi <- rsi
	rsi <<= 1
	rsi <- rsi
	rsi += 1
	rsi <- rsi
	rsi += 6
	rdi <- rsi
	rsi <- 1
	call allocate 2
	rdi <- rax
	mem rsp 24 <- rdi
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += 8
	mem rdi 0 <- 5
	rdi <- mem rsp 24
	rsi <- rdi
	rsi += 16
	rdi <- mem rsp 16
	mem rsi 0 <- rdi
	rdi <- mem rsp 24
	rsi <- rdi
	rsi += 24
	rdi <- mem rsp 0
	mem rsi 0 <- rdi
	rsi <- 0
	rdi <- 1
	rsi <- rsi
	rsi += 0
	rsi <- rsi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 43
	rsi <- 0
	rdi <- 1
	rsi <- rsi
	rsi += 1
	rsi <- rsi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 73
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 2
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 29
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 3
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 33
	rsi <- 0
	rdi <- 1
	rsi <- rsi
	rsi += 4
	rsi <- rsi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 47
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 5
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 43
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 6
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 81
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 7
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 33
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 8
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 63
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 9
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 13
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 10
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 63
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 11
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 413
	rsi <- 0
	rdi <- 1
	rsi <- rsi
	rsi += 12
	rsi <- rsi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 93
	rdi <- 0
	rsi <- 1
	rdi <- rdi
	rdi += 13
	rsi <- rdi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 123
	rsi <- 0
	rdi <- 1
	rsi <- rsi
	rsi += 14
	rsi <- rsi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	mem rdi 0 <- 14353
	rdi <- 0
	mem rsp 8 <- rdi
	rdi <- mem rsp 16
	rdi <- rdi
	mem rsp 16 <- rdi
	rdi <- mem rsp 16
	rdi >>= 1
	mem rsp 16 <- rdi
	goto :call_label1
	:call_label1
	rsi <- 0
	rdi <- 1
	rsi <- rsi
	rdi <- mem rsp 8
	rsi += rdi
	rsi <- rsi
	rsi *= 8
	rsi <- rsi
	rsi += 24
	rdi <- mem rsp 24
	rdi <- rdi
	rdi += rsi
	rdi <- mem rdi 0
	rdi <- rdi
	rdi <<= 1
	rdi <- rdi
	rdi += 1
	rdi <- rdi
	call print 1
	rdi <- mem rsp 8
	rdi <- rdi
	mem rsp 8 <- rdi
	rdi <- mem rsp 8
	rdi += 1
	mem rsp 8 <- rdi
	rsi <- mem rsp 8
	rdi <- mem rsp 16
	rdi <- rsi < rdi
	cjump rdi = 1 :call_label1
	goto :call_label2
	:call_label2
	return
)

)
