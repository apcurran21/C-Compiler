(@go
(@go
	0 0
	mem rsp -8 <- :ret
	call @myF 0
	:ret
	return
)

(@myF
	0 1
	rdi <- 1
	r10 <- 1
	rdx <- 1
	rcx <- 1
	rsi <- 1
	mem rsp 0 <- rsi
	r11 <- 1
	rsi <- 1
	r9 <- 1
	r8 <- 1
	rdi <- rdi
	rdi <- r10
	rdi <- rdx
	rdi <- rcx
	rdi <- mem rsp 0
	rdi <- rdi
	rdi <- r11
	rdi <- rsi
	rdi <- r9
	rdi <- r8
	return
)

)
