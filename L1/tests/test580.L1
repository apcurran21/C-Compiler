(@entry
(@entry
	0 3
	rsi <- 40
	rdi <- rsi
	mem rsp 8 <- rdi
	rdi <- rsi
	rdi <<= 1
	rdi += 1
	rsi <- -1
	rsi <- rsi
	call allocate 2
	rdi <- rax
	mem rsp 0 <- rdi
	rdi <- 0
	rsi <- rdi
	:loop_start
	rdx <- rsi
	rdx <<= 1
	rdx += 1
	rcx <- rsi
	rcx <<= 3
	rdi <- mem rsp 0
	rcx += rdi
	mem rcx 8 <- rdx
	mem rsp -8 <- :loop_inc
	rdi <- rsi
	mem rsp 16 <- rdi
	rdi <- rsi
	call @loop_inc_gen 1
	:loop_inc
	rsi <- rax
	rdi <- mem rsp 16
	rdi <- rdi
	rdi += rsi
	rsi <- rdi
	rdi <- mem rsp 8
	cjump rsi < rdi :loop_start
	goto :loop_end
	:loop_end
	rdi <- mem rsp 0
	rdi <- rdi
	call print 1
	return
)

(@loop_inc_gen
	1 0
	rsi <- 1
	rdi &= 1
	cjump rdi = 0 :is_dbt
	goto :not_dbt
	:is_dbt
	rsi <- 3
	:not_dbt
	rax <- rsi
	return
)

)
